MEMORY
{
	RAM: start = 0, size = $10000, fillval=$00;
}

SEGMENTS
{
	CODE: load = RAM, start = $400, type = ro;
	GAKUFU: load = RAM, start = $1400, type = ro;
	WAVE_SOD: load = RAM, start = $3900, type = ro;
	WAVE_DIR: load = RAM, start = $3e00, type = ro;
}
